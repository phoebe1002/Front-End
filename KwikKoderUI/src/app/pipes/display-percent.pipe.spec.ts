import { DisplayPercentPipe } from './display-percent.pipe';

describe('DisplayPercentPipe', () => {
  it('create an instance', () => {
    const pipe = new DisplayPercentPipe();
    expect(pipe).toBeTruthy();
  });
});
